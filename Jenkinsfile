
@Library('shared-library') _
pipeline {
    agent any
    stages {
        stage('welcome') {
            steps {
                helloDevOpsWorld(name: 'Shared Library Workshop Attendees')
            }
        }
    }
}
